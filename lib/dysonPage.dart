import 'package:flutter/material.dart';

class DysonPage extends StatelessWidget {
  const DysonPage({super.key});
  @override
  Widget build(BuildContext context) {
      return const Center(
        child: Text('No favourites yet.'),
      );
  }
}